declare module 'maath/random/dist/maath-random.esm' {
    export function inSphere(arr: Float32Array, options: { radius: number }): Float32Array;
    // Add other functions as needed from the maath/random module
  }
  